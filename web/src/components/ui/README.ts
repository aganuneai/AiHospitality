/**
 * @layer ui — Internal Primitive Layer (Shadcn/Radix)
 *
 * These are base primitives — unstyled or minimally styled building blocks.
 * They should NOT be used directly in pages or features.
 *
 * RULES:
 * ✅ Neo components (`/neo`) may import from here
 * ❌ Pages and features MUST NOT import from here directly
 *
 * To use a component in a page, check if a Neo wrapper exists:
 *   - Button  → neo-button
 *   - Card    → neo-card
 *   - Input   → neo-input
 *   - Table   → neo-table
 *   - Sheet   → neo-sheet
 *
 * If no Neo wrapper exists, create one instead of importing ui/ directly.
 */
